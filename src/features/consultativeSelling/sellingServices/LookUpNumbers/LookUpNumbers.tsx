import { Text, View } from "react-native";

// type Props = StackScreenProps<RootStackParamList, "Selling/LookUpNumbers">;
// const LookUpNumbers = ({ route }: Props): React.ReactElement => {
const LookUpNumbers = (): React.ReactElement => {
  return (
    <View>
      <Text>ABCD</Text>
    </View>
  );
};
