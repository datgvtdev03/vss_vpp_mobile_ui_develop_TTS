const prefix = "@vpp";

export default {
  language: prefix + "language",
  theme: prefix + "theme",
  userInfo: prefix + "userInfo",
  lastAccessApp: prefix + "lastAccessApp",
  thermalPrinter: prefix + "thermalPrinter",
  computerPrint: prefix + "computerPrint",
};
